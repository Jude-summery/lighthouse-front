export interface LoginModelState {
    name: string
}

export interface LoginModelType {

}

const Login = {
    namespace: 'Login',
    state: {
      name: 'xiazz'
    }
  }
  
export default Login
